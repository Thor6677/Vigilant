{
  "components": [
    "design",
    "web",
    "performance"
  ],
  "discoveredDate": "2026-05-13",
  "id": "ISS-004",
  "impact": "`<script src=\"https://cdn.tailwindcss.com\">` (base.html:9) loads the Tailwind JIT runtime (~80KB compressed) on every page load. Tailwind's own docs flag this as dev-only. Three downstream problems: (1) blocks first-paint until the runtime compiles utilities, causing the FOUC pattern users may see between the inline `<style>` block applying and the first Tailwind utility class painting; (2) introduces a third-party origin used for the inline-config evaluation, complicating the planned CSP `unsafe-inline` removal (ties directly to T-012); (3) the in-file tailwind.config (base.html:11-32) declares `eve.bg/surface/border/accent/gold/danger/success/muted` but most templates use mixed `b-*` semantic classes — Tailwind utilities and the b-* system are both shipping, only one is needed. Recommended: replace with the official `@tailwindcss/cli` build during the Docker image build, output a minified CSS file served from `/static/`, drop the CDN script. Coordinates with T-012 (CSP).",
  "location": [
    "app/templates/base.html:9-33"
  ],
  "phase": "performance-and-ops",
  "relatedTickets": [
    "T-012"
  ],
  "resolution": "Replaced cdn.tailwindcss.com runtime + inline `tailwind.config` with a build-time `@tailwindcss/cli` v4 step. New `frontend/tailwind.input.css` re-declares the `eve-*` color palette and `font-mono` family via `@theme` and uses class-based `dark` via `@custom-variant`. The Dockerfile frontend stage copies `app/templates/` into `./tailwind-content/` so Tailwind can scan templates for utility classes used; output is written to `dist/tailwind.css` and copied into the runtime image at `/app/static/css/tailwind.css`. `base.html` now serves the minified CSS via a single `<link rel=\"stylesheet\" href=\"/static/css/tailwind.css\">`. Local build verified: 21 KB minified output containing all `eve-*` utilities and the `md:`/`lg:`/`hover:` variants used in templates. CDN third-party origin removed, simplifying the planned CSP `unsafe-inline` work in T-012. The `b-*` semantic system in `base.html`'s `<style>` block is unchanged; the duplication mentioned in the impact will be addressed separately.",
  "resolvedDate": "2026-05-13",
  "severity": "high",
  "status": "resolved",
  "title": "Tailwind via CDN ships in production"
}
