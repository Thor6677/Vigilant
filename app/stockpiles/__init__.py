"""Stockpile watchlists (Phase 5 Task 3).

`holdings` — pure holdings/deficit math + CRUD + per-user holdings aggregation.
`alerts`   — the background checker that emits `stockpile_low` notifications.
"""
