/**
 * Shared style constants for the Jump Planner and Gate Route Planner panels.
 * Both planners use the same panel chrome (font, background, borders, etc.)
 * but distinct accent colors so they're visually distinguishable.
 */

export const FONT = "'JetBrains Mono', monospace";
export const BG = 'rgba(14, 14, 14, 0.97)';
export const BORDER = '#191919';
export const TEXT = '#dedede';
export const MUTED = '#474747';
export const ACCENT = '#c8a951';

/** Jump Drive Planner accent (orange). */
export const JUMP_COLOR = '#ff8800';

/** Gate Route Planner accent (cyan, matches ROUTE_COLOR in utils/constants.ts). */
export const GATE_COLOR = '#00d4ff';
